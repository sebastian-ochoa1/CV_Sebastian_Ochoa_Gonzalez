import React from 'react';

const H1withText = ({ title, text }) => {
  return (
    <div>
      <div>
        <h1>{tittle}</h1>
      </div>
      <div>
        <p>{text}</p>
      </div>
    </div>
  );
};

export default H1withText;
